# ──────────────────────────────────────────────────────────────────
# ai_service/requirements.txt
# Python dependencies — Realtime Wave Observer AI Microservice
#
# Install:  pip install -r requirements.txt
# Build:    pyinstaller --onefile --name ai_engine main.py
# ──────────────────────────────────────────────────────────────────

# ── Web Framework ──────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.34.0    # ASGI server (includes websockets, httptools)

# ── Data & Signal Processing ───────────────────────────────────────
numpy>=2.0.0
pandas>=2.2.0
scipy>=1.13.0

# ── Machine Learning ───────────────────────────────────────────────
# CPU-only PyTorch for offline deployment (smaller bundle size ~500MB)
# For GPU support, replace with the appropriate CUDA wheel:
#   torch --index-url https://download.pytorch.org/whl/cu124
torch>=2.4.0

# ── Utility ────────────────────────────────────────────────────────
pydantic>=2.7.0              # Schema validation (bundled with FastAPI)
python-multipart>=0.0.9      # Form data / file upload support

# ── Build / Packaging ──────────────────────────────────────────────
pyinstaller>=6.8.0           # Bundle Python app into a standalone .exe
