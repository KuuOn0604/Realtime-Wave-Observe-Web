/**
 * frontend/src/test/setup.ts
 * Vitest global setup — chạy TRƯỚC mỗi test file.
 *
 * - Import @testing-library/jest-dom để extend Vitest matchers:
 *   toBeInTheDocument(), toHaveTextContent(), toBeVisible(), v.v.
 */
import '@testing-library/jest-dom';
